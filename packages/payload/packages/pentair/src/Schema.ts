import { asSchema } from '@xyo-network/payload-model'

export type PentairScreenlogicSchema = typeof PentairScreenlogicSchema
export const PentairScreenlogicSchema = asSchema('network.xyo.pentair.screenlogic', true)
